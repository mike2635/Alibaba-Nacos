server:
  port: 8001
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        service: nacos-consumer
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  application:
    name: nacos-consumer
  profiles:
    active: dev
